import {AppBar, Grid, Toolbar } from "@mui/material";

const MyAppBar = () => {
    return ( 
        <>
        <AppBar>
            <Toolbar>
                <Grid container>
                    <Grid item></Grid>
                </Grid>
            </Toolbar>
        </AppBar>
        </>
     );
}
 
export default AppBar;